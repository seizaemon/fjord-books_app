<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>
<h1><%= t('views.common.title_new', name: Book.model_name.human) %></h1>

<%= render 'form', book: @book %>

<%= link_to t('views.common.back'), books_path %>
