<p id="notice"><%= notice %></p>

<p><%= link_to "英/日 切替", book_path(params[:id], locale: @locale) %></p>

<p>
  <strong><%= t Book.human_attribute_name(:title) + ":" %></strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t Book.human_attribute_name(:memo) + ":" %></strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t Book.human_attribute_name(:author) + ":" %></strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t Book.human_attribute_name(:picture) + ":" %></strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t(:edit), edit_book_path(@book) %> |
<%= link_to t(:back), books_path %>
